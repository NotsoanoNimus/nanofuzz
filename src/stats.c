/*
 * stats.c
 *
 * Implementations of API hooks for recording and understanding fuzzing
 *   statistics from this library's integration into another application.
 *
 */

#include "stats.h"



// Define the struct used by StatsBlockPtr_t in the header file.
struct _fuzz_stats_block_t {
    
};
